name: 🌙 Nacht-Import: place_ids_archive.json

on:
  schedule:
    - cron: '0 2 * * *' # ⏰ Täglich um 02:00 UTC = 04:00 MEZ (Sommerzeit)

  workflow_dispatch: # ▶️ Manuell auslösbar für Tests

jobs:
  import:
    runs-on: ubuntu-latest

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      PLACE_IDS_FILE: place_ids_archive.json

    steps:
      - name: 📥 Repository klonen
        uses: actions/checkout@v3

      - name: 🧰 Node.js einrichten
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Abhängigkeiten installieren
        run: npm install @supabase/supabase-js node-fetch dotenv

      - name: ▶️ Import-Script ausführen (Nachtlauf)
        run: node scripts/import_places.js
